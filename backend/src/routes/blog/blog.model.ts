export interface Post {
  name: string,
  category: string,
  description: string,
  content: string,
  created: string
}

export interface SlimPost {
  name: string,
  category: string,
  created: string
}
